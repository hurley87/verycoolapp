

<%= form_for @pin, html: { multipart: true } do |f| %>
  <% if @pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
      <% @pin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :boss %><br>
    <%= f.text_field :boss %>
  </div>

  <div class="field">
    <%= f.label :quote %><br>
    <%= f.text_field :quote %>
  </div>

  <div class="form-group">
    <%= f.label :image %><br>
    <%= f.file_field :image, class: "form-control btn-primary" %>
  </div>

  <div class="field">
    <%= f.label :story %><br>
    <%= f.text_area :story, :rows => 5 %>
  </div>
 
     <div class="form-group"><%= f.submit "Publish", class: "btn btn-primary" %></div>

<% end %>
